# Connections saved here do not appear in the UI
# Postgres example using the airflow database
pg_conn:
  conn_type: postgres
  host: postgres
  login: airflow
  password: airflow
  schema: airflow

# Remote MySQL source (VPN required from Docker host)
# Replace placeholder values before use.
mysql_unsta:
  conn_type: mysql
  host: 10.0.0.10
  port: 3306
  login: your_user
  password: your_password
  schema: eUNSTAv3
